# NeiHei 1.0 架构审计报告与修复清单

> 审计人：Cline（十年架构师视角）
> 审计日期：2026-05-18
> 审计范围：核心执行引擎、状态管理层、插件系统、画布渲染层、UI组件层

---

## 🔴 P0 级：必须立即修复（否则项目无法稳定运行）

### P0-1：画布拖拽添加节点「零校验」— 拖入未注册类型直接崩溃
**文件：** `src/canvas/Canvas.handlers.ts:24`
**现状：** `handleDropEvent` 只校验了 `pluginType` 非空，未校验该类型是否已在插件注册表中注册。如果侧边栏数据损坏或插件加载失败，创建 type=undefined 的节点 -> React Flow 渲染崩溃。
**修复策略：** 在 `handleDropEvent` 中添加 `pluginRegistry.getManifest(type)` 校验，未注册时弹出错误 toast 并阻止创建。
**影响范围：** 画布拖放功能

### P0-2：数据流引擎（dataflow/engine.ts）已启动但不工作
**文件：** `src/dataflow/engine.ts:97-107`
**现状：** `initDataflowEngine()` 函数体内只打印日志，没有任何实际的 `subscribe` 调用。且该函数**从未在 App.tsx 或 main.tsx 中被调用过**。同时 `propagateFromNode` 虽然是完整实现的，但从未被触发。
**修复策略：** 
1. 在 App.tsx 的 `useEffect` 中调用 `initDataflowEngine()`
2. 在 `initDataflowEngine` 中实际写入 `useCanvasStore.subscribe` 逻辑
**影响范围：** 核心数据流传播功能

### P0-3：OutlineNode 执行状态从未同步到 execution-store
**文件：** `src/chajian/OutlineNode/OutlinePanel.tsx`
**现状：** `handleRunAI` 和 `runAutoSequence` 只更新了 `useWorldEditorFlowStore` 的 `stepStatus`，但从未调用 `useExecutionStore.setSuccess/setError`。导致画布上 OutlineNode 的边框颜色始终为灰色，无执行反馈。
**修复策略：** 在 AI 调用成功/失败的分支中，同步调用 `useExecutionStore` 的 `setSuccess`/`setError`。
**影响范围：** 世界编辑器节点的执行状态可视化

### P0-4：插件加载失败「有 error log 无用户提示」
**文件：** `src/plugin-system/plugin-loader.ts:148-156` + `src/App.tsx:108-112`
**现状：** 所有加载异常都有 try-catch + console.error，但没有任何 UI 层面的用户提示。app 初始化时加载插件，如果某个 manifest.json 损坏，用户完全看不到错误。
**修复策略：** 在 App.tsx 的插件加载完成后，将失败结果写入 log-store，让 LogPanel 可看到；同时在画布上显示加载统计。
**影响范围：** 插件系统用户体验

### P0-5：TypeScript 类型安全 — Record<string, any> 数据泄露
**文件：** 
- `src/chajian/OutlineNode/OutlinePanel.tsx:63` — `buildContext()` 返回 `Record<string, any>`
- `src/store/execution-store.ts:16` — `result?: any`
- `src/dataflow/engine.ts:75` — 直接写入 `_aiLoading`, `_aiError` 等 magic keys
**现状：** `any` 类型在核心数据流中蔓延，破坏类型安全。
**修复策略：** 
1. 将 `execution-store.ts` 的 `result` 更改为 `result?: unknown`，消费者自行类型断言
2. 规范 node.data 中的 magic keys 为接口中的可选字段
**影响范围：** 代码质量与维护性

---

## 🟡 P1 级：影响功能完整性和用户体验

### P1-1：无撤销/重做
**文件：** 全项目
**现状：** 搜索 `undo`、`redo`、`history`、`Command` 相关实现，完全不存在。用户误删节点将无法恢复。
**修复策略：** 在 canvas-store 上实现命令栈模式的撤销/重做，或考虑使用 `@xyflow/react` 的内置 undo/redo。
**影响范围：** 用户体验

### P1-2：异步操作无锁 — AI 调用期间可重复点击执行
**文件：** `src/App.tsx:42-98` + `src/chajian/OutlineNode/OutlinePanel.tsx:308`
**现状：** `handleRun` 函数在 `setRunState('running')` 前没有检查当前状态，可并发调用。自动模式中 `runAutoSequence` 调用 AI 无取消机制。
**修复策略：** 添加防重入锁（基于 `useRef` 或 store 中的 `isRunning`），在 handleRun 顶部立即检查并 return。
**影响范围：** 数据完整性

### P1-3：节点连线规则缺失
**文件：** `src/store/canvas-store.ts:72`
**现状：** `onConnect` 使用 `addEdge` 直接添加边，没有任何校验：未校验端口类型匹配、未防止循环依赖、未限制同节点自连接。
**修复策略：** 在 `onConnect` 中添加连接校验器，使用图算法（DFS）检测循环依赖。
**影响范围：** 工作流正确性

### P1-4：本地存储版本历史可能丢失
**文件：** `src/store/useWorldEditorFlowStore.ts:90`
**现状：** `partialize` 只持久化了 `mode/currentStep/steps`，`stepStatus`（信号灯状态）没有被持久化。刷新页面后运行状态丢失。
**修复策略：** 在 `partialize` 中添加 `stepStatus` 或在页面加载时重置为非运行状态。
**影响范围：** 世界编辑器流程持久性

---

## 🔵 P2 级：代码可维护性

### P2-1：Canvas.tsx 严重超行（~550 行源码）
**文件：** `src/canvas/Canvas.tsx`
**现状：** 包含浮窗管理、置顶面板、右键菜单（节点级+面板级+浮窗级×2）、快速连接菜单、双击创建菜单、快捷键处理、拖拽处理等混杂逻辑。
**修复策略：** 拆分为 CanvasCore（纯渲染）+ CanvasFloatingManager（浮窗）+ CanvasMenuManager（菜单）+ CanvasConnectManager（连接）
**影响范围：** 维护效率

### P2-2：硬编码值
**文件：** 散布
- `dataflow/engine.ts:22` — `THROTTLE_MS = 800` 硬编码节流延迟
- `Canvas.handlers.ts:24` — `'application/plugin-type'` 硬编码 MIME 类型
- `Canvas.tsx:520` — `pluginType === 'outline'` 硬编码节点类型
### P2-3：无单元测试
**文件：** 全项目
**现状：** 项目中无 `.test.ts` 或 `.spec.ts` 文件。核心执行引擎和插件加载器 0 测试覆盖。

---

## 修复顺序与工作量估算

| 执行顺序 | 优先级 | 问题 | 预计工时 | 依赖关系 |
|----------|--------|------|----------|----------|
| 1 | 🔴P0-1 | 拖拽未注册插件崩溃 | 0.5h | 无 |
| 2 | 🔴P0-2 | 数据流引擎不工作 | 2h | 无 |
| 3 | 🔴P0-3 | OutlineNode 执行状态不同步 | 1h | 无 |
| 4 | 🔴P0-4 | 插件加载失败无用户提示 | 0.5h | 无 |
| 5 | 🔴P0-5 | TypeScript any 类型蔓延 | 1h | 无 |
| 6 | 🟡P1-2 | 异步操作无锁 | 0.5h | 无 |
| 7 | 🟡P1-3 | 连线规则缺失 | 2h | 无 |
| 8 | 🟡P1-4 | 流程状态持久化不完整 | 0.5h | P0-3 |
| 9 | 🟡P1-1 | 无撤销/重做 | 4h | P0-2 |
| 10 | 🔵P2-1 | Canvas.tsx 拆分 | 2h | 无 |
| 11 | 🔵P2-2 | 硬编码值清理 | 1h | 无 |
| 12 | 🔵P2-3 | 测试覆盖 | 3h | P0-2, P0-4 |
