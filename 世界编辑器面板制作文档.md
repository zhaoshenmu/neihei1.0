# 世界编辑器面板制作文档

## 一、当前架构总结

### 现有组件及问题

| 组件 | 功能 | 现状问题 |
|------|------|---------|
| **PromptSquare.tsx** | 提示词广场浮动面板，5个按钮 → 弹出 WorldEditorPrompt 子面板 | 内容仅存 localStorage，与 OutlinePanel 完全隔离 |
| **WorldEditorPrompt.tsx** | 600×700 文本编辑器，支持版本历史 | 纯文本编辑，没有模板变量解析功能 |
| **OutlinePanel.tsx** | 世界编辑器5个标签页容器 | 仅做标签切换，无页面间通信和运行按钮 |
| **PageSetting.tsx** | 作品设定表单（创意/视角/主角/风格/字数） | useState 本地管理，无数据导出 |
| **PageWorld.tsx** | 世界构建表单（力量体系/物理规则/社会架构等） | 同上，无 AI 填充 |
| **PageCharacter.tsx** | 人物核心表单（角色列表+欲望/缺陷/弧光） | 同上 |
| **PagePlot.tsx** | 剧情大纲表单（故事总纲+三幕结构） | 同上 |
| **PageConsistency.tsx** | 一致性检查报告 | 静态数据，无 AI 驱动 |
| **usePanelDataStore.ts** | Zustand + persist 面板数据存储 | 基础框架可用，需扩展 |
| **ai-service.ts** | AI 大模型调用服务 | 可直接复用 |
| **OutlineNode/index.tsx** | 画布上的节点UI | 需要加胶囊开关 |

---

## 二、需要创建的新文件（4个）

```
src/
├── config/
│   └── world_editor_schema.json       # 面板字段配置 JSON
├── store/
│   ├── usePromptStore.ts              # 提示词存储（localStorage + 默认提示词）
│   └── useWorldEditorFlowStore.ts     # 世界编辑器流程状态（当前步骤、模式等）
└── utils/
    └── prompt-template.ts             # 模板引擎：替换 {{变量}} 占位符
```

### 2.1 `world_editor_schema.json`

定义5个面板的所有表单字段配置，包括字段类型（text/textarea/radio/select）、选项值等。前端根据此 JSON 动态渲染表单。

### 2.2 `usePromptStore.ts`

- 存储结构：`{ "setting": { content: string, variables: string[], updatedAt: number }, ... }`
- 内置5个默认提示词模板（含 `{{变量}}` 占位符）
- 当用户未自定义时回退使用默认提示词
- 在 PromptSquare 保存提示词时同步更新到此 store

### 2.3 `useWorldEditorFlowStore.ts`

管理世界编辑器的运行状态：

```typescript
interface WorldEditorFlowState {
  mode: 'auto' | 'manual';              // 自动/手动模式
  currentStep: number;                   // 当前步骤索引（0-based）
  steps: TabId[];                       // ['setting','world','character','plot','consistency']
  isRunning: boolean;                   // 是否正在运行
  setMode: (mode: 'auto' | 'manual') => void;
  goToStep: (index: number) => void;
  nextStep: () => TabId | null;         // 进入下一步
  runCurrentStep: () => Promise<void>;  // 执行当前步骤的AI生成
}
```

### 2.4 `prompt-template.ts`

模板引擎函数：

```typescript
function renderPrompt(template: string, context: Record<string, any>): string
// 将 {{世界名称}} → context.世界名称，支持多层嵌套
```

---

## 三、需要修改的现有文件（9个）

| 文件 | 修改内容 |
|------|---------|
| **OutlineNode/index.tsx** | 在输出口（trigger-out）右侧居中位置添加自动/手动胶囊开关 |
| **OutlinePanel.tsx** | 每个标签页内容区域右上角添加 [▶] 小运行按钮（手动模式显示）；集成流程控制 |
| **PageSetting.tsx** | 表单数据改用 usePanelDataStore；用户填写，无AI生成按钮 |
| **PageWorld.tsx** | 表单数据改用 usePanelDataStore；接收 AI 填充数据；右上角 [▶] 按钮 |
| **PageCharacter.tsx** | 同上 |
| **PagePlot.tsx** | 同上 |
| **PageConsistency.tsx** | 右上角 [▶] 按钮 → 调用 AI 生成一致性报告 + 输出最终JSON |
| **PromptSquare.tsx** | 保存提示词时同步写入 usePromptStore |
| **TopToolbar.tsx** | "▶ 运行"按钮行为：手动模式只触发第1页，自动模式逐页运行全部 |

---

## 四、UI 布局变化

### 4.1 画布上的 OutlineNode（世界编辑器节点）

```
          ┌────────────────────────┐
          │  作品设定                │
          │  世界构建                │     ┌──────────┐
          │  人物核心    ○ ○ ○      │     │  ⚡ 手动 │
          │  剧情大纲                │     └──────────┘
          │  一致性检查              │
          └────────────────────────┘
              ↑                    ↑
          输入端口          输出端口   胶囊开关在输出口右侧居中
```

### 4.2 双击节点打开的 OutlinePanel（世界编辑器面板）

```
┌──────────────────────────────────────────────────────────────┐
│ [作品设定] [世界构建] [人物核心] [剧情大纲] [一致性检查]         │
├──────────────────────────────────────────────────────────────┤
│                                                      [▶]    │
│  作品设定表单（用户手工填写）                                   │
│  创意、视角、主角、风格、字数...                                │
├──────────────────────────────────────────────────────────────┤
│  第2页：世界构建                                      [▶]    │
│  第3页：人物核心                                      [▶]    │
│  第4页：剧情大纲                                      [▶]    │
│  第5页：一致性检查                                    [▶]    │ → 最终输出JSON
└──────────────────────────────────────────────────────────────┘
```

---

## 五、操作流程

### 5.1 手动模式

| 步骤 | 操作 | 结果 |
|------|------|------|
| 0 | 用户在画布节点上切换胶囊为"手动" | 模式切换 |
| 1 | 用户打开面板，手动填写第1页（作品设定） | 表单数据存入 usePanelDataStore |
| 2 | 点击画布"▶ 运行"按钮 | 后台打包第1页数据→AI生成第2页→自动跳转到第2页标签 |
| 3 | 用户查看/修改第2页，确认后点击第2页右上角 [▶] | AI生成第3页→自动跳转到第3页 |
| 4 | 用户查看/修改第3页，点击第3页右上角 [▶] | AI生成第4页→自动跳转到第4页 |
| 5 | 用户查看/修改第4页，点击第4页右上角 [▶] | AI生成第5页（一致性报告）→自动跳转到第5页 |
| 6 | 用户查看报告，点击第5页右上角 [▶] | 输出完整 world_final.json，通过 trigger-out 传给下一个节点 |

### 5.2 自动模式

| 步骤 | 操作 | 结果 |
|------|------|------|
| 0 | 用户在画布节点上切换胶囊为"自动" | 模式切换 |
| 1 | 用户填写第1页（作品设定） | 表单数据存入 usePanelDataStore |
| 2 | 点击画布"▶ 运行"按钮 | 自动逐页执行全部5页：生成第2页→填充→生成第3页→填充→...→第5页→输出JSON |

---

## 六、数据流示意图

```
PromptSquare（提示词广场）
    ↓ 用户编辑保存
usePromptStore（提示词存储，含默认提示词）
    ↓
用户填写第1页（作品设定）
    ↓ 数据存入
usePanelDataStore（所有面板表单数据）
    ↓ 点击运行按钮
模板引擎 renderPrompt + 所有已有表单数据
    ↓ 构造完整prompt
callAi()（AI服务）
    ↓ 返回JSON
解析JSON填充下一页表单（usePanelDataStore）
    ↓ 自动切换标签
下一页继续 ...
    ↓ 最终
一致性检查（收集全部数据 → AI综合分析 → 评分报告）
    ↓ 
world_final.json（完整世界观JSON）
    ↓ 通过 trigger-out 端口
画布上下一个连接的节点
```

---

## 七、实施步骤顺序

### 第1步：创建基础文件（4个新文件）
1. `src/config/world_editor_schema.json`
2. `src/store/usePromptStore.ts`（含5个默认提示词模板）
3. `src/store/useWorldEditorFlowStore.ts`
4. `src/utils/prompt-template.ts`

### 第2步：改造 OutlineNode（加胶囊开关）
- 修改 `src/chajian/OutlineNode/index.tsx`
- 在输出口右侧居中位置添加自动/手动切换胶囊

### 第3步：改造 PromptSquare（同步提示词）
- 修改 `src/components/PromptSquare.tsx`
- 保存提示词时同步写入 usePromptStore

### 第4步：改造各页面组件（接入 store + 运行按钮）
- 修改 `PageSetting.tsx`（只用store，不加运行按钮）
- 修改 `PageWorld.tsx`（store + 右上角 [▶] + AI填充）
- 修改 `PageCharacter.tsx`（同上）
- 修改 `PagePlot.tsx`（同上）
- 修改 `PageConsistency.tsx`（store + 右上角 [▶] + AI报告 + JSON输出）

### 第5步：改造 OutlinePanel（流程控制）
- 修改 `src/chajian/OutlineNode/OutlinePanel.tsx`
- 集成 useWorldEditorFlowStore
- 每页右上角渲染 [▶] 按钮（手动模式显示）
- 跑完最后一页输出 JSON

### 第6步：改造 TopToolbar（运行行为）
- 修改 `src/components/TopToolbar.tsx`
- 手动模式：只触发第1页
- 自动模式：触发全部5页

### 第7步：联调测试
- 验证手动模式逐页流程
- 验证自动模式全自动流程
- 验证提示词同步
- 验证最终 JSON 输出

---

## 八、关键设计决策

1. **第1页（作品设定）不生成AI内容**，由用户手工填写，这是整个流程的起点
2. 每页的 [▶] 按钮在**内容区域右上角**，标签栏下方，对齐美观
3. 胶囊开关在**画布节点**上，不在面板内，方便切换模式
4. 自动模式下点击画布"▶ 运行"后，隐藏所有 [▶] 按钮，由流程控制器自动执行
5. 所有表单数据统一通过 `usePanelDataStore` 管理，实现跨页面通信
